using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VideoPlayerWebGL : MonoBehaviour
{
    //public string pathName = "MenuLoop.mp4"; 

    // Start is called before the first frame update
    void Start()
    {
        /*
        var videoPlayer = gameObject.GetComponent<UnityEngine.Video.VideoPlayer>();

        videoPlayer.url = System.IO.Path.Combine(Application.streamingAssetsPath, pathName); 

        videoPlayer.Play();
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
